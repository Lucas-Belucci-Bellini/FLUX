/**
 * @flux/ui - the FLUX design system.
 *
 * Everything visible in the product is built from these parts and the tokens
 * in `styles/tokens.css`. A page that needs a colour or a spacing the system
 * does not have is a signal to extend the system, not to write a one-off.
 */

export * from './cn';
export * from './components/Button';
export * from './components/primitives';
export * from './components/Theme';
